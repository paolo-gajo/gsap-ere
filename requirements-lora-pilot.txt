# Direct runtime dependencies for the Argo H100 pilot.
# gsapere is installed separately with --no-deps because only its evaluator is used.
accelerate==1.14.0
huggingface_hub==1.18.0
numpy==2.4.2
peft==0.20.0
safetensors==0.7.0
sentence-transformers==6.0.0
torch==2.12.0
transformers==5.16.1
