# Direct runtime dependencies verified against Argo's Python 3.11 wheelhouse.
# gsapere is installed with --no-deps in a Python 3.10 .eval-env because only
# its evaluator is used and its declared Transformers constraint conflicts.
accelerate==1.13.0
huggingface_hub==1.18.0
numpy==2.4.2
peft==0.19.1
safetensors==0.7.0
sentence-transformers==5.1.2
torch==2.12.0
transformers==5.10.2
